import { createSlice } from "@reduxjs/toolkit";
import {} from "../store";

const initialState = {};

const homestaySlice = createSlice({
  name: "homestaySlice",
  initialState,
  reducers: {},
});

export const {} = homestaySlice.actions;

export default homestaySlice.reducer;
