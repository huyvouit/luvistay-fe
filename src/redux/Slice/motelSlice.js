import { createSlice } from "@reduxjs/toolkit";
import {} from "../store";

const initialState = {};

const motelSlice = createSlice({
  name: "motelSlice",
  initialState,
  reducers: {},
});

export const {} = motelSlice.actions;

export default motelSlice.reducer;
